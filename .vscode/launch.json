{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run clever_lama as module",
            "type": "python",
            "request": "launch",
            "module": "src.main",
            "cwd": "${workspaceFolder}"
        }
    ]
}